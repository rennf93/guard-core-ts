{
  "name": "guardcore-ts",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript port of guard-core — framework-agnostic security middleware engine",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "~6.0"
  },
  "packageManager": "pnpm@10.25.0"
}
